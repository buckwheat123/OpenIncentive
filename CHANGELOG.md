# 更新日志 / Changelog

本项目版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)，遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 规范。

## [v4.2] — 2026-09-25

在 v4.1 基础上的一次收敛性调整：重新梳理「计算触发 / 通知信导入 / 计划命名 / 模板协作 / 预填导出」五处口径与权限，并修复一个已计算数据被误显示为「未计算」的缺陷。

### 新增 / Added

- **通知信权重完整性校验（F1）** — 通知信（年度格式）数据导入在预览与执行两处均按 `(员工, 期间, 计划)` 汇总权重：只要某员工的**任一计划权重合计不等于 100%**（容差 1e-6），该员工的**全部行整批拒绝、不予导入**，其余员工照常导入。计算大表导入不受此约束（其口径仍是加权加总、绝不归一化），仅记录 `weight_total_pct` 并在导出批注列提示。
- **模板共享与协作（F4）** — 通知信模板在所有管理员（平台 ADMIN 与全部 BG 管理员）及 `Global` 模板之间**完全共享可见**；**他人创建的模板以只读方式呈现**，可「复制并编辑」派生一份归属自己的副本（自动加「（副本）」后缀、落到创建者的主属 BG），随后自由改写；**创建者可删除自己的模板，平台 ADMIN 可删除任意模板**。列表新增「创建者」列及「我的 / 只读」标记，新增 `/letters/templates/{tid}/copy`、`/letters/templates/{tid}/delete` 两个受权限约束的路由。
- **季度预填大表导出（F6）** — 后台导入页可将权限范围内、指定季度的现有计划与目标**预先填成一张季度大表**下载，供业务分析与下一季度计算直接改写后再上传；`import.html` 增补用途说明，模板下拉纳入全部可用期间。

### 变更 / Changed

- **计划命名唯一化（F5）** — 同一**期间内（跨 BG 全局）**，一套独特的 `(KPI, Curve, 权重)` 组合对应唯一一个计划名（Quota 不计入签名）。导入时：若传入的组合与既有计划一致但名称不同，**自动改用既有计划名并提示改名**；若传入的名称已被不同组合占用，**按传入顺序自动追加 `_v1` / `_v2` 后缀**。预览与执行基于同一份数据库快照解析，结果确定一致。

### 移除 / Removed

- **两步式 CSV 触发计算模块（F3）** — 删除后台 `/admin/calc` 及其预览页与模板生成函数；触发计算统一由一键计算的 `/admin/runs` 承担。

### 修复 / Fixed

- **已计算数据被显示为「未计算」（F2）** — 视图层的取数逻辑此前只看每个期间的**最后一个计算批次**，一旦封存后再次触发（跳过全部人员、生成空批次）就会让已封存人员凭空变回「未计算」。改为在**同一期间的全部批次**中按 `(员工[, 计划])` 取最新一条结果，BG / 团队 / 个人视图不再误报。

### 测试 / Testing

- 端到端测试扩充并全绿：新增一键计算、封存后再算仍显示已计算（F2）、计划改名 / 自动加后缀（F5）、通知信权重非 100% 整员工拒绝（F1）、模板只读 / 复制 / 各自删除 / 平台 ADMIN 任意删除（F4）、季度预填大表导出（F6）；冲突用例调整为「Q2 冲突拦截 + Q3 一致忽略 + 2029 新计划成功导入 1 个新版本」，以体现通知信导入绝不覆盖既有季度计算、但全新计划照常落库的口径。

### 备注 / Notes

- 数据库仍以 `create_all` 建表、无迁移机制——**改动模型后需重新运行 `seed.py` 重建库**。
- 加权支付率口径不变：`Σ(权重% × 支付率) ÷ 100`，按原状加权加总、绝不归一化；系统不提供未加权（简单平均）支付率。计算大表仍允许权重合计≠100%（仅标注），100% 强约束只作用于通知信导入。

## [v4.1] — 2026-09-12


在 v4（季度大表两步式导入 + 逐行校验 + 触发计算 + 特殊调整 + 封存 + 双表导出 + 角色化查看 + 代操作 + 批量导入用户）基础上的一次特性发布：新增业务连续性工具、导入与导出的健壮性增强、BG 多对多与员工信息版本化，以及通知信模块的全面升级。

### 新增 / Added

- **一键快照与还原（业务连续性）** — `backup.py` 通过 SQLite 在线备份 API 对数据库做一致性拷贝，连同会话签名密钥与本地发件箱归档到 `backups/<时间戳>_<标签>/`，并写入 manifest（git 版本、完整性检查、各表行数）；滚动保留最近 N 份（`BACKUP_KEEP` / `--keep`，默认 20），过期快照移入回收站而非物理删除。`restore.py` 还原前校验完整性、提示代码/结构漂移、要求先停服，并总是先对当前状态做一次「还原前快照」，让错误的还原本身也可撤销。（`e42c3fd`）
- **BG 多对多** — 用户与 BG 通过 `UserManagedBg` 关联表建立多对多关系：一名 BG 管理员可管理多个 BG，一个 BG 也可有多名管理员。`User.bg` 保留为首个主属 BG 以兼容既有分区逻辑；BG 视图合并展示所有受管 BG 的成员，导出文件名带范围标签；`/admin/users/{uid}/managed_bgs` 可重新同步受管 BG 集合。（`d8c3a62`）
- **员工信息版本化** — 批量导入用户或大表导入将覆盖某个人的身份字段前，先把旧记录快照为一条停用的 `UserVersion`（新信息启用、旧信息停用封存），`/person` 页向 BG 管理员 / 平台管理员开放版本历史。（`d8c3a62`）
- **通知信模块全面升级（Batch E1）** — 章节更名为「通知信及计划信息导入」；`Plan_Table` 只呈现计划结构（KPI / 权重 / 目标 / Curve，不含实绩与支付率），新增 `Performance_Table` 逐 KPI 呈现权重 / 目标 / 实绩 / 达成率 / Curve / 原始支付率 / 加权贡献及锁定的加权支付率汇总行；占位符可替换全部数据表字段（`NAME / EMPLOYEE_ID / EMAIL / BG / DEPARTMENT / JOB_TITLE / MANAGER / PERIOD / PLAN_NAME / MESSAGE / PLAN_TABLE / PERFORMANCE_TABLE / CURVE_SUMMARY`）；平台 ADMIN 撰写的 `Global` 模板对所有 BG 管理员可见、可另存为（fork）但不可直接改写；平台 ADMIN 可对任意收件人群发。（`d3c054c`）
- **年度格式的通知信数据导入（Batch E2a）** — 通知信数据导入改用年度格式：每行一个 KPI + `year` + 四个 YTD 目标列（`ytd_q1~ytd_q4`），系统按年度展开成各季度计划后复用现有大表解析 / 最新覆盖 / 版本化流程，空白季度跳过；`errors.csv` 映射回原始年度行。（`56feca7`）
- **通知信 vs 季度计算冲突拦截（Batch E2b）** — 年度导入执行二次传递时，若展开后的 `(期间, 员工, 计划, KPI)` 已有 current 计划且目标值不同，预览阶段即标记「与季度计算数据冲突」并拦截，执行阶段以 `conflict_check=True` 复核确保不写入；非冲突行照旧最新覆盖。新增独立冲突清单 `/letters/data/conflicts.csv`（映射回原始年度行，含四个 `existing_qN` 列与逐季度「旧 vs 新」说明），预览页在存在冲突时高亮提示并提供下载按钮。以季度计算数据为准，通知信导入绝不静默覆盖季度计算结果。（`d49618b`）

### 变更 / Changed

- **季度大表导入：表内后行覆盖前行** — 同一 `(期间 + 员工 + 计划 + KPI)` 的重复行，封存前不再报错，而是后出现的行覆盖先前值（后值优先），并合并跨行的员工信息更新。（`af684fe`）
- **批量导入用户：支持同表新建上级 + 管理员代理** — 允许在同一张花名册表内定义全新上级（前向引用可解析）；当被引用上级在系统中尚不存在时，回退指向一名平台 ADMIN 作为临时上级代理，而非直接拒绝该行。（`af684fe`）
- **多 BG 一键封存** — 封存支持勾选多个 BG 一次性执行（含全选），并汇总「新封存 / 已封存」结果。（`af684fe`）
- **导出纳入已封存记录** — 导出取「同一期间、同一 (员工, 计划) 在所有批次里的最新结果」，因此最后一次计算后被封存的记录仍会导出（不再丢失），两张表新增 `sealed` 列（Y/空），各 KPI 明细表新增 `weighted_contribution_pct`（权重% × 支付率）。（`6ae94cb`）
- **批次详情改为列表** — 每人由卡片块改为单张结果表，每行可展开其逐 KPI 明细。面向用户的 Quota 标签更名为「YTD Quota / Target」。（`6ae94cb`）
- **用户管理：批量停用 / 启用** — 平台 ADMIN 可批量退休 / 恢复用户（跳过当前操作者自身账号），每次变更均写入审计日志。（`af684fe`）
- **README 与示例更新** — README 覆盖批次 A~E 特性、演示账号（BGA1 双 BG、BGA2 共管）、BG 多对多与员工信息版本化规则、年度格式导入与冲突拦截、通知信 Global 模板与两张占位符、新增「备份·还原·空白起步」运维章节，测试步数 22→28；`sample_data/letter_data.csv` 改写为年度格式示例；提交并文档化 `reset_empty.py`（空白起步脚本）。（`1cbe810`）

### 测试 / Testing

- 端到端测试扩充至 28 步并全绿（新增：多 BG 封存、后行覆盖实绩、新建上级 / 管理员代理联动、批量启停、多 BG 管理员范围、员工信息版本化归档、受管 BG 重新同步、Plan/Performance 两表拆分与 Global 模板只读、年度模板下载 / 预填 / 展开、冲突拦截与冲突清单）。

### 备注 / Notes

- 数据库仍以 `create_all` 建表、无迁移机制——**改动模型后需重新运行 `seed.py` 重建库**。
- 加权支付率口径锁定为 `Σ(权重% × 支付率) ÷ 100`，按原状加权加总、绝不归一化；系统不提供未加权（简单平均）支付率。

[v4.1]: https://github.com/buckwheat123/OpenIncentive/compare/75a22cb...HEAD
[v4.2]: https://github.com/buckwheat123/OpenIncentive/compare/f587e2d...HEAD
